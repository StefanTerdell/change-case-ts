{
  "name": "@stefan/change-case-ts",
  "exports": "./src/lib.ts",
  "version": "0.2.0",
  "license": "ISC",
  "tasks": {
    "dev": "deno run --watch src/lib.ts",
    "npm": "deno run -A scripts/build-npm.ts",
    "readme": "deno run -A scripts/template-readme.ts",
    "dry": "deno test && deno task npm && deno task readme && deno publish --dry-run && cd npm && npm publish --dry-run",
    "pub": "deno test && deno task npm && deno task readme && deno publish && cd npm && npm publish"
  },
  "imports": {
    "@deno/dnt": "jsr:@deno/dnt@^0.42.3",
    "@std/assert": "jsr:@std/assert@1"
  },
  "description": "Type-aware case changer for strings and properties",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/StefanTerdell/change-case-ts.git"
  }
}
